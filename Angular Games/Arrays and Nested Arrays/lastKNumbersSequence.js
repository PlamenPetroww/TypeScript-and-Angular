function solve(x, y) {

    let sum = 0;
    let anotherSum = 0;
    let res = [];
    let onlyOne = 1;

    for (let i = 0; i < x; i++) {
        if (i < 1) {
            res.push(onlyOne)
            continue;
        }
        
        sum = 0;
    }
}

solve(6, 3)